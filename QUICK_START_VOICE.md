# 🚀 Quick Start - Voice APIs

## البدء السريع مع Voice APIs

### 1️⃣ التثبيت والإعداد

```bash
# تثبيت dependencies
npm install

# إعداد environment variables
cp .env.example .env
```

### 2️⃣ إضافة API Keys في `.env`

```env
# AssemblyAI (للتحويل من صوت إلى نص)
ASSEMBLYAI_API_KEY=your_assemblyai_key_here

# OpenRouter (للذكاء الاصطناعي - مفضّل)
OPENROUTER_API_KEY=your_openrouter_key_here

# Gemini (احتياطي)
GEMINI_API_KEY=your_gemini_key_here
```

### 3️⃣ تشغيل السيرفر

```bash
npm run dev
```

### 4️⃣ اختبار Voice APIs

```bash
# اختبار شامل لجميع Voice APIs
npm run test:voice-apis

# اختبار الذكاء الاصطناعي
npm run test:ai
```

---

## 🎤 استخدام ONE-CLICK API (الأسهل)

### مثال باستخدام cURL:

```bash
curl -X POST http://localhost:3000/api/voice/process-complete \
  -H "Authorization: Bearer YOUR_JWT_TOKEN" \
  -F "audio_file=@recording.mp3" \
  -F "language=ar" \
  -F "auto_create_tasks=true" \
  -F "auto_create_notes=true"
```

### مثال باستخدام JavaScript:

```javascript
const formData = new FormData();
formData.append('audio_file', audioFile);
formData.append('language', 'ar');
formData.append('auto_create_tasks', 'true');
formData.append('auto_create_notes', 'true');

const response = await fetch('http://localhost:3000/api/voice/process-complete', {
  method: 'POST',
  headers: {
    'Authorization': `Bearer ${token}`
  },
  body: formData
});

const result = await response.json();
console.log(result);
```

---

## 📝 أمثلة صوتية للاختبار

### مثال 1: مهمة بسيطة
**قل في التسجيل:**
> "عايز أشتري لبن وخبز بكرة الصبح"

**النتيجة المتوقعة:**
- ✅ مهمة واحدة: "شراء مستلزمات"
- ✅ مهمتين فرعيتين: "شراء لبن"، "شراء خبز"
- ✅ التاريخ: غداً الساعة 9 صباحاً

---

### مثال 2: مهمة عاجلة
**قل في التسجيل:**
> "مهم جداً أتصل بالدكتور الساعة 3 العصر"

**النتيجة المتوقعة:**
- ✅ مهمة واحدة: "الاتصال بالدكتور"
- ✅ الأولوية: عالية (high)
- ✅ الوقت: اليوم الساعة 3 مساءً

---

### مثال 3: مهام وملاحظات
**قل في التسجيل:**
> "محتاج أجهز العرض التقديمي للاجتماع يوم الأحد. نوت: لازم أركز على الأرقام والإحصائيات"

**النتيجة المتوقعة:**
- ✅ مهمة واحدة: "تجهيز العرض التقديمي"
- ✅ ملاحظة واحدة: "ملاحظة عن العرض التقديمي"
- ✅ تاجات تلقائية: ["عرض_تقديمي", "اجتماع", "ملاحظة"]

---

## 🎯 اللهجات المدعومة

### اللهجة المصرية
```
عايز أشتري لبن بكرة
محتاج أروح الشغل النهاردة
لازم أخلص المشروع دلوقتي
```

### اللهجة السعودية/الخليجية
```
أبغى أشتري حليب باجر
ودي أروح الدوام اليوم
لازم أخلص المشروع الحين
```

### اللهجة الشامية
```
بدي اشتري حليب بكرا
لازم روح الشغل اليوم
لازم خلص المشروع هلق
```

---

## 📚 التوثيق الكامل

للتوثيق الشامل، راجع:
- 📖 `docs/VOICE_API_DOCUMENTATION.md` - توثيق كامل لجميع APIs
- 📊 `VOICE_IMPROVEMENTS.md` - شرح التحسينات المُنفذة
- 🧪 `test-voice-apis.js` - أمثلة اختبار شاملة

---

## ❓ الأسئلة الشائعة

### س: ما هي أفضل جودة للتسجيل الصوتي؟
**ج:** جودة متوسطة (128kbps) كافية. الأهم هو وضوح الصوت وعدم وجود ضوضاء خلفية.

### س: هل يدعم النظام اللهجات المختلفة؟
**ج:** نعم! يدعم 4 لهجات عربية رئيسية: المصرية، السعودية/الخليجية، الشامية، والمغربية.

### س: كيف أفرّق بين المهام والملاحظات؟
**ج:** استخدم كلمة "نوت" أو "ملاحظة" قبل الملاحظات. المهام تبدأ بأفعال مثل "عايز"، "محتاج"، "لازم".

### س: هل يمكن تعديل النص بعد التحويل؟
**ج:** نعم! استخدم endpoint `/update-transcription` لتعديل النص قبل استخراج المهام.

### س: ما هو الحد الأقصى لحجم الملف؟
**ج:** 10MB (يمكن تغييره في `config/index.js`)

---

## 🆘 الدعم

إذا واجهت أي مشكلة:
1. تأكد من إضافة API Keys في `.env`
2. تأكد من تشغيل السيرفر: `npm run dev`
3. راجع logs في console
4. راجع التوثيق الكامل في `docs/`

---

Made with ❤️ by Voclio Team
