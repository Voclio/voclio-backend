# 🎙️ Voclio API - Voice Notes & Task Management System

نظام متكامل لإدارة المهام والملاحظات الصوتية مع ذكاء اصطناعي

## ✨ الميزات الرئيسية

### 🎤 التسجيلات الصوتية
- تحويل الصوت إلى نص (Speech-to-Text)
- استخراج المهام والملاحظات تلقائياً من الصوت
- دعم اللغة العربية والإنجليزية
- معالجة ذكية باستخدام AI

### ✅ إدارة المهام
- إنشاء وتعديل وحذف المهام
- المهام الفرعية (Subtasks)
- الأولويات والحالات
- التصنيفات والتاريخ المحدد
- إحصائيات المهام

### 📝 الملاحظات
- إنشاء ملاحظات من الصوت أو النص
- الوسوم (Tags)
- البحث والتصفية
- ربط الملاحظات بالتسجيلات الصوتية

### 🔔 نظام الإشعارات (جديد!)
- إشعارات تلقائية لجميع الأحداث المهمة
- تتبع المهام القريبة والمتأخرة
- إشعارات التذكيرات
- إشعارات الإنجازات والإنتاجية
- أولويات متعددة (low, normal, high, urgent)
- [📖 دليل نظام الإشعارات الكامل](./NOTIFICATION_SYSTEM.md)

### ⏰ التذكيرات
- تذكيرات مرتبطة بالمهام
- إشعارات متعددة (Email, Push, WhatsApp)
- تأجيل التذكيرات (Snooze)

### 🎯 الإنتاجية
- جلسات التركيز (Focus Sessions)
- سلاسل الإنجازات (Streaks)
- الإنجازات (Achievements)
- ملخصات الإنتاجية
- اقتراحات AI للتحسين

### 🔐 المصادقة والأمان
- تسجيل الدخول بالبريد وكلمة المرور
- OAuth (Google, Facebook)
- JWT Tokens
- OTP للتحقق
- إعادة تعيين كلمة المرور

### 👨‍💼 لوحة الإدارة
- إدارة المستخدمين
- إحصائيات النظام
- سجلات النشاط
- إدارة API Keys

## 🚀 البدء السريع

### المتطلبات
- Node.js >= 18.0.0
- PostgreSQL
- npm أو yarn

### التثبيت

```bash
# 1. استنساخ المشروع
git clone <repository-url>
cd voclio-api

# 2. تثبيت المكتبات
npm install

# 3. إعداد ملف البيئة
cp .env.example .env
# قم بتعديل .env بالإعدادات الخاصة بك

# 4. إعداد قاعدة البيانات
npm run init-db

# 5. تشغيل migration الإشعارات
npm run migrate:notifications

# 6. تشغيل السيرفر
npm run dev
```

### أو استخدام Makefile

```bash
# إعداد كامل
make setup

# تشغيل التطوير
make dev

# تشغيل الإنتاج
make start
```

## 📡 API Endpoints

### المصادقة
- `POST /api/auth/register` - تسجيل مستخدم جديد
- `POST /api/auth/login` - تسجيل الدخول
- `POST /api/auth/oauth/google` - تسجيل دخول Google
- `POST /api/auth/refresh` - تحديث Token

### المهام
- `GET /api/tasks` - جلب جميع المهام
- `POST /api/tasks` - إنشاء مهمة
- `PUT /api/tasks/:id` - تحديث مهمة
- `PUT /api/tasks/:id/complete` - تحديد مهمة كمكتملة
- `DELETE /api/tasks/:id` - حذف مهمة
- `GET /api/tasks/stats` - إحصائيات المهام

### التسجيلات الصوتية
- `POST /api/voice/upload` - رفع تسجيل صوتي
- `POST /api/voice/transcribe` - تحويل صوت إلى نص
- `POST /api/voice/process-complete` - معالجة كاملة (رفع + تحويل + استخراج)
- `GET /api/voice/recordings` - جلب التسجيلات

### الإشعارات 🔔
- `GET /api/notifications` - جلب جميع الإشعارات
- `GET /api/notifications/unread-count` - عدد الإشعارات غير المقروءة
- `PUT /api/notifications/:id/read` - تحديد إشعار كمقروء
- `PUT /api/notifications/mark-all-read` - تحديد الكل كمقروء
- `DELETE /api/notifications/:id` - حذف إشعار

### التذكيرات
- `GET /api/reminders` - جلب التذكيرات
- `POST /api/reminders` - إنشاء تذكير
- `PUT /api/reminders/:id/snooze` - تأجيل تذكير

### الإنتاجية
- `POST /api/productivity/focus-session` - بدء جلسة تركيز
- `PUT /api/productivity/focus-session/:id/end` - إنهاء جلسة
- `GET /api/productivity/streak` - جلب السلسلة
- `GET /api/productivity/achievements` - جلب الإنجازات

[📖 دليل API الكامل](./API_COMPLETE_REFERENCE.md)

## 🔧 إصلاحات قاعدة البيانات

إذا واجهت مشاكل في قاعدة البيانات، استخدم هذه الأوامر:

```bash
# إصلاح جميع ENUM types (يحل مشاكل casting)
npm run fix:enums

# إصلاح timestamps في جميع الجداول
npm run fix:timestamps

# إصلاح schema الإشعارات
npm run migrate:notifications

# فحص حالة ENUM types
npm run check:enums

# فحص التصنيفات
npm run check:categories
```

**المشاكل الشائعة:**
- ❌ `cannot cast type character varying to enum_*` → `npm run fix:enums`
- ❌ `column "created_at" does not exist` → `npm run fix:timestamps`
- ❌ `column "priority" does not exist` → `npm run migrate:notifications`

[📖 دليل إصلاحات قاعدة البيانات الكامل](./DATABASE_FIXES_GUIDE.md)

## 🧪 الاختبار

```bash
# اختبار الاتصال بقاعدة البيانات
npm run test-db

# اختبار نظام الإشعارات
npm run test:notifications

# اختبار التسجيلات الصوتية
npm run test:voice
```

## 📚 التوثيق

- [📖 دليل API الكامل](./API_COMPLETE_REFERENCE.md)
- [📡 جميع الـ APIs](./ALL_APIS_COMPLETE.md)
- [📮 دليل Postman](./POSTMAN_GUIDE.md)
- [📱 دليل Mobile App API](./MOBILE_APP_API_GUIDE.md)
- [👨‍💼 دليل لوحة الإدارة](./ADMIN_PANEL_GUIDE.md)
- [🎤 دليل Voice to Everything](./VOICE_TO_EVERYTHING.md)
- [🔔 نظام الإشعارات](./NOTIFICATION_SYSTEM.md)
- [🗄️ دليل إصلاحات قاعدة البيانات](./DATABASE_FIXES_GUIDE.md)
- [📚 فهرس التوثيق](./DOCUMENTATION_INDEX.md)
- [🚀 البدء السريع](./QUICK_START.md)

## 🔧 التكوين

### متغيرات البيئة الأساسية

```env
# Server
PORT=3001
NODE_ENV=development

# Database
DB_HOST=localhost
DB_PORT=5432
DB_NAME=voclio_db
DB_USER=postgres
DB_PASSWORD=your_password

# JWT
JWT_SECRET=your_secret_key
JWT_REFRESH_SECRET=your_refresh_secret

# AI Services
GEMINI_API_KEY=your_gemini_key
OPENROUTER_API_KEY=your_openrouter_key
ASSEMBLYAI_API_KEY=your_assemblyai_key

# OAuth
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_secret

# Email
EMAIL_HOST=smtp.gmail.com
EMAIL_USER=your_email@gmail.com
EMAIL_PASSWORD=your_app_password
```

## 🎯 نظام الإشعارات التلقائية

النظام يرسل إشعارات تلقائياً عند:

✅ **المهام**
- إنشاء مهمة جديدة
- تحديث مهمة
- إكمال مهمة
- مهمة قريبة (خلال 24 ساعة)
- مهمة متأخرة

🎤 **التسجيلات الصوتية**
- معالجة تسجيل بنجاح
- إنشاء مهمة من الصوت

🔔 **التذكيرات**
- إنشاء تذكير جديد
- حلول موعد التذكير

⏱️ **الإنتاجية**
- إكمال جلسة تركيز
- الوصول لمعالم السلسلة (كل 7 أيام)
- الحصول على إنجاز

👤 **النظام**
- ترحيب بمستخدم جديد
- تغيير كلمة المرور
- تأكيد البريد الإلكتروني

## 🤖 Cron Jobs

النظام يشغل مهام تلقائية في الخلفية:

- **كل دقيقة**: فحص التذكيرات المستحقة
- **كل ساعة**: فحص المهام القريبة والمتأخرة
- **يومياً**: تنظيف البيانات المنتهية

## 🛠️ التطوير

```bash
# تشغيل وضع التطوير
npm run dev

# تشغيل migrations
npm run migrate:notifications

# تنظيف الملفات المؤقتة
make clean
```

## 📦 البناء والنشر

```bash
# بناء Docker image
make docker-build

# تشغيل Docker containers
make docker-up

# إيقاف Docker containers
make docker-down
```

## 🤝 المساهمة

نرحب بالمساهمات! يرجى:
1. Fork المشروع
2. إنشاء branch للميزة الجديدة
3. Commit التغييرات
4. Push إلى Branch
5. فتح Pull Request

## 📄 الترخيص

MIT License

## 📞 الدعم

للمساعدة والاستفسارات، يرجى فتح Issue في GitHub.

---

صنع بـ ❤️ للمطورين العرب
