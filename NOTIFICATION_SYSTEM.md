# 🔔 نظام الإشعارات - Voclio Notification System

## نظرة عامة

نظام إشعارات شامل يتتبع جميع الأحداث المهمة في التطبيق ويرسل إشعارات للمستخدمين تلقائياً.

## ✨ الميزات

### 1. إشعارات المهام (Tasks)
- ✅ **إنشاء مهمة جديدة** - عند إنشاء أي مهمة
- 📝 **تحديث مهمة** - عند تعديل تفاصيل المهمة
- ✔️ **إكمال مهمة** - عند وضع علامة مكتمل على المهمة
- ⏰ **موعد قريب** - تنبيه قبل 24 ساعة من موعد المهمة
- ⚠️ **مهمة متأخرة** - تنبيه للمهام التي تجاوزت موعدها

### 2. إشعارات التسجيلات الصوتية (Voice)
- 🎤 **معالجة التسجيل** - عند تحويل الصوت إلى نص بنجاح
- ✨ **إنشاء مهمة من الصوت** - عند إنشاء مهمة تلقائياً من التسجيل

### 3. إشعارات التذكيرات (Reminders)
- 🔔 **تذكير جديد** - عند إنشاء تذكير
- ⏰ **تفعيل التذكير** - عند حلول موعد التذكير

### 4. إشعارات الإنتاجية (Productivity)
- ⏱️ **جلسة تركيز مكتملة** - عند إنهاء جلسة تركيز
- 🔥 **سلسلة إنجازات** - عند الوصول لمعالم مهمة (كل 7 أيام)
- 🏆 **إنجاز جديد** - عند الحصول على إنجاز

### 5. إشعارات النظام (System)
- 👋 **ترحيب** - عند تسجيل مستخدم جديد
- 🔒 **تغيير كلمة المرور** - عند تحديث كلمة المرور
- ✅ **تأكيد البريد** - عند تأكيد البريد الإلكتروني

## 📊 أنواع الأولويات

```javascript
priority: 'low'      // منخفضة - إشعارات عامة
priority: 'normal'   // عادية - معظم الإشعارات
priority: 'high'     // عالية - تذكيرات ومهام قريبة
priority: 'urgent'   // عاجلة - مهام متأخرة
```

## 🎯 أنواع الإشعارات

```javascript
type: 'general'      // عام
type: 'task'         // متعلق بالمهام
type: 'reminder'     // تذكيرات
type: 'achievement'  // إنجازات
type: 'system'       // نظام
```

## 🔄 الإشعارات التلقائية (Cron Jobs)

### كل دقيقة
- ✅ فحص التذكيرات المستحقة

### كل ساعة
- 📅 فحص المهام القريبة (خلال 24 ساعة)
- ⚠️ فحص المهام المتأخرة
- 🧹 تنظيف OTP منتهية الصلاحية

### يومياً (منتصف الليل)
- 🧹 تنظيف الجلسات المنتهية

## 📡 API Endpoints

### جلب جميع الإشعارات
```http
GET /api/notifications
Query Parameters:
  - page: رقم الصفحة (افتراضي: 1)
  - limit: عدد النتائج (افتراضي: 20)
  - is_read: تصفية حسب القراءة (true/false)
```

### عدد الإشعارات غير المقروءة
```http
GET /api/notifications/unread-count
```

### جلب إشعار محدد
```http
GET /api/notifications/:id
```

### تحديد إشعار كمقروء
```http
PUT /api/notifications/:id/read
```

### تحديد جميع الإشعارات كمقروءة
```http
PUT /api/notifications/mark-all-read
```

### حذف إشعار
```http
DELETE /api/notifications/:id
```

## 💻 استخدام NotificationService

### في الكود

```javascript
import NotificationService from '../services/notification.service.js';

// إشعار مهمة جديدة
await NotificationService.notifyTaskCreated(userId, task);

// إشعار مهمة مكتملة
await NotificationService.notifyTaskCompleted(userId, task);

// إشعار مهمة قريبة
await NotificationService.notifyTaskDueSoon(userId, task, hoursLeft);

// إشعار مهمة متأخرة
await NotificationService.notifyTaskOverdue(userId, task);

// إشعار تذكير
await NotificationService.notifyReminderTriggered(userId, reminder, task);

// إشعار جلسة تركيز
await NotificationService.notifyFocusSessionCompleted(userId, session);

// إشعار إنجاز
await NotificationService.notifyAchievementEarned(userId, achievement);

// إشعار ترحيب
await NotificationService.notifyWelcome(userId, userName);
```

## 🗄️ هيكل قاعدة البيانات

```sql
CREATE TABLE notifications (
    notification_id SERIAL PRIMARY KEY,
    user_id INTEGER REFERENCES users(user_id),
    title VARCHAR(255),
    message TEXT,
    type VARCHAR(50),
    priority VARCHAR(50) DEFAULT 'normal',
    is_read BOOLEAN DEFAULT false,
    read_at TIMESTAMP,
    related_id INTEGER,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

## 📱 مثال Response

```json
{
  "success": true,
  "data": [
    {
      "notification_id": 1,
      "user_id": 123,
      "title": "مهمة جديدة",
      "message": "تم إنشاء مهمة جديدة: إنهاء التقرير",
      "type": "task",
      "priority": "normal",
      "is_read": false,
      "read_at": null,
      "related_id": 456,
      "created_at": "2024-01-31T10:30:00Z"
    }
  ],
  "pagination": {
    "page": 1,
    "limit": 20,
    "total": 45,
    "unread_count": 12
  }
}
```

## 🎨 أيقونات الإشعارات المقترحة

- 📝 مهمة جديدة
- ✅ مهمة مكتملة
- ⏰ موعد قريب
- ⚠️ مهمة متأخرة
- 🔔 تذكير
- 🎤 تسجيل صوتي
- ⏱️ جلسة تركيز
- 🔥 سلسلة إنجازات
- 🏆 إنجاز جديد
- 👋 ترحيب
- 🔒 أمان

## 🔧 التكوين

جميع الإشعارات تعمل تلقائياً بدون تكوين إضافي. يتم إرسال الإشعارات عند:

1. ✅ إنشاء/تحديث/إكمال المهام
2. 🎤 معالجة التسجيلات الصوتية
3. 🔔 إنشاء التذكيرات أو حلول موعدها
4. ⏱️ إكمال جلسات التركيز
5. 🏆 الحصول على إنجازات
6. 👤 أحداث المستخدم (تسجيل، تغيير كلمة مرور)

## 📈 الإحصائيات

يمكن الحصول على إحصائيات الإشعارات:

```javascript
const stats = await NotificationService.getNotificationStats(userId);
// Returns:
// {
//   total: 100,
//   unread: 15,
//   read: 85,
//   unread_percentage: 15
// }
```

## 🚀 التحسينات المستقبلية

- [ ] إشعارات Push للموبايل
- [ ] إشعارات WhatsApp
- [ ] تخصيص أنواع الإشعارات لكل مستخدم
- [ ] إشعارات صوتية
- [ ] تجميع الإشعارات المتشابهة
- [ ] جدولة الإشعارات

## 📝 ملاحظات

- جميع الإشعارات تُحفظ في قاعدة البيانات
- يمكن للمستخدم تحديد الإشعارات كمقروءة
- الإشعارات مرتبطة بالعناصر ذات الصلة (related_id)
- نظام الأولويات يساعد في ترتيب الإشعارات
- Cron jobs تعمل تلقائياً في الخلفية
