# 🚀 Quick Start - Voice-to-Everything

## ✅ What's New?

تم إضافة **One-Click Voice-to-Everything** endpoint الذي يعمل كل حاجة في طلب واحد:

1. ✅ رفع التسجيل الصوتي
2. ✅ تحويل الصوت لنص (AssemblyAI)
3. ✅ استخراج المهام والملاحظات بالذكاء الاصطناعي
4. ✅ إنشاء المهام مع المهام الفرعية تلقائياً
5. ✅ إنشاء الملاحظات مع الـ Tags
6. ✅ ربط كل حاجة بالكالندر

---

## 🎯 الـ Endpoint الجديد

```
POST /api/voice/process-complete
```

### مثال بسيط (cURL):

```bash
curl -X POST http://localhost:3001/api/voice/process-complete \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -F "audio=@recording.mp3" \
  -F "language=ar" \
  -F "auto_create_tasks=true" \
  -F "auto_create_notes=true"
```

---

## 🤖 تحسينات الذكاء الاصطناعي

### 1. استخراج التواريخ من العربي

الـ AI دلوقتي يفهم:
- **بكرة** → اليوم التالي
- **بعد بكرة** → بعد يومين
- **الأسبوع الجاي** → بعد 7 أيام
- **يوم السبت** → أقرب يوم سبت
- **الساعة 5** → 5:00 مساءً
- **3 مساءً** → 3:00 PM

### 2. استخراج المهام الفرعية تلقائياً

**مثال:**
```
Voice: "محتاج أشتري لبن وخبز وجبنة"
```

**النتيجة:**
- مهمة رئيسية: "التسوق"
- 3 مهام فرعية تلقائياً

### 3. تحديد الأولوية الذكي

- **مهم، ضروري، عاجل، لازم** → Priority: HIGH
- **ممكن، لو فاضي** → Priority: LOW
- الباقي → Priority: MEDIUM

---

## 📁 الملفات الجديدة

1. **`src/services/ai.service.js`** - تحسينات الـ AI
2. **`src/controllers/voice.controller.js`** - الـ endpoint الجديد
3. **`src/utils/dateParser.js`** - استخراج التواريخ من العربي
4. **`VOICE_TO_EVERYTHING.md`** - التوثيق الكامل
5. **`VOICE_TO_EVERYTHING_POSTMAN.json`** - Postman Collection
6. **`test-voice-to-everything.js`** - Test Script

---

## 🧪 اختبار الميزة

### الطريقة 1: استخدام Postman

1. افتح Postman
2. Import الملف: `VOICE_TO_EVERYTHING_POSTMAN.json`
3. حدّث الـ `accessToken` variable
4. جرّب الـ endpoint: **"Process Voice Complete"**

### الطريقة 2: استخدام Test Script

```bash
node test-voice-to-everything.js
```

### الطريقة 3: cURL

```bash
# 1. Login أولاً
curl -X POST http://localhost:3001/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"your@email.com","password":"yourpassword"}'

# 2. استخدم الـ Token
curl -X POST http://localhost:3001/api/voice/process-complete \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -F "audio=@your-audio.mp3" \
  -F "language=ar"
```

---

## 📝 مثال عملي

### Input (Voice):
```
"محتاج أخلص البروجكت بتاع الشغل بكرة الساعة 5
وكمان لازم أروح السوبر ماركت أشتري لبن وخبز وجبنة
وعندي ميتينج مع العميل يوم الأحد الساعة 3
ومحتاج أكتب نوت عن الأفكار الجديدة للمشروع"
```

### Output:
```json
{
  "created": {
    "tasks": [
      {
        "title": "إنهاء البروجكت بتاع الشغل",
        "priority": "high",
        "due_date": "2026-02-01T17:00:00Z",
        "subtasks": []
      },
      {
        "title": "التسوق من السوبر ماركت",
        "priority": "high",
        "subtasks": [
          { "title": "شراء لبن" },
          { "title": "شراء خبز" },
          { "title": "شراء جبنة" }
        ]
      },
      {
        "title": "ميتينج مع العميل",
        "priority": "medium",
        "due_date": "2026-02-02T15:00:00Z"
      }
    ],
    "notes": [
      {
        "title": "أفكار المشروع الجديد",
        "content": "محتاج أكتب نوت عن الأفكار الجديدة للمشروع",
        "tags": ["ideas", "project"]
      }
    ]
  }
}
```

---

## 🔧 المتطلبات

تأكد إن الـ API Keys موجودة في `.env`:

```env
# Required for transcription
ASSEMBLYAI_API_KEY=your_key_here

# Required for AI extraction (one of them)
OPENROUTER_API_KEY=your_key_here
# OR
GEMINI_API_KEY=your_key_here
```

---

## 📅 ربط الكالندر

كل المهام اللي ليها `due_date` بتظهر تلقائياً في الكالندر:

```bash
# عرض كالندر الشهر
GET /api/calendar/month/2026/2

# عرض أحداث يوم معين
GET /api/calendar/day/2026-02-01
```

---

## 🎯 Best Practices

1. **اتكلم بوضوح** عشان الـ transcription يكون دقيق
2. **اذكر التواريخ والأوقات** بشكل واضح
3. **استخدم "و"** لفصل العناصر في القوائم
4. **قول "مهم" أو "ضروري"** للمهام ذات الأولوية العالية
5. **قول "نوت" أو "ملاحظة"** لإنشاء ملاحظات بدل مهام

---

## 🐛 حل المشاكل

### Transcription Failed
- تأكد من وجود `ASSEMBLYAI_API_KEY` في `.env`
- تأكد إن الملف الصوتي بصيغة مدعومة (MP3, WAV, M4A)
- تأكد إن حجم الملف أقل من 10 MB

### AI Extraction Failed
- تأكد من وجود `OPENROUTER_API_KEY` أو `GEMINI_API_KEY`
- تحقق من الـ API quota
- شوف الـ server logs للتفاصيل

### Tasks Not Created
- تأكد إن `auto_create_tasks=true`
- تأكد إن الـ transcription نجح
- تحقق إن الـ AI استخرج مهام

---

## 📚 التوثيق الكامل

اقرأ الملف: **`VOICE_TO_EVERYTHING.md`** للتفاصيل الكاملة

---

## 🎉 جاهز للاستخدام!

السيرفر شغال على: `http://localhost:3001`

جرّب الـ endpoint الجديد وشوف السحر! ✨
